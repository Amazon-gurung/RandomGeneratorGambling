import SMLink from "./SMLink";

const SMIcon = () => {
  return (
    <section className="flex flex-col justify-center items-center w-full min-h-full">
      <SMLink />
    </section>
  );
};

export default SMIcon;
